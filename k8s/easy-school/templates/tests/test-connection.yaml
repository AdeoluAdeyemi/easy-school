apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "easy-school.fullname" . }}-test-connection"
  labels:
    {{- include "easy-school.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "easy-school.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
